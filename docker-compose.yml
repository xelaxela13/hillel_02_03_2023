version: "3.1"

services:
  backend:
    build: .
    command: tail -f > /dev/null
#    command: ["python", "manage.py", "runserver", "0.0.0.0:8000"]
    volumes:
      - .:/project
    ports:
      - 8000:8000
    restart: always
